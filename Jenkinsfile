pipeline {
        stages {
            stage('build') {
                steps {
                    cmakeBuild(
                        installation: 'InSearchPath'
                    )
                }
            }
        }   
}
